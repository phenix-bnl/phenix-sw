AC_INIT(configure.in)

AM_INIT_AUTOMAKE(pisaRoot, 1.00)
AC_PROG_CXX(CC g++)
AC_ENABLE_STATIC(NO)
AC_PROG_LIBTOOL

dnl AC_FIND_ROOT
ROOTLIBS=`root-config --libs`
AC_SUBST(ROOTLIBS)

AC_OUTPUT(Makefile)

