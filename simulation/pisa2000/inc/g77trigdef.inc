      real cosd
      real sind
      real tand
      real acosd
      real asind
      real atand
      real atan2d

      external cosd
      external sind
      external tand
      external acosd
      external asind
      external atand
      external atan2d
