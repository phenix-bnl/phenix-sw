AC_INIT(configure.in)

AM_INIT_AUTOMAKE(rpcgeom, 1.00)

AC_PROG_CXX(CC g++)
AC_ENABLE_STATIC(no)
AC_PROG_LIBTOOL

dnl NEVER remove the $CXXFLAGS from the CXXFLAGS setting
dnl enable all warnings, make warning fatal
if test $ac_cv_prog_gxx = yes; then
  CXXFLAGS="$CXXFLAGS -Wall -Werror"
fi

AC_OUTPUT(Makefile)
