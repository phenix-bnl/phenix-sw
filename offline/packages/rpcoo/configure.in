AC_INIT(configure.in)

AM_INIT_AUTOMAKE(rpcoo, 1.00)

AC_PROG_CXX(CC g++)
AC_ENABLE_STATIC(no)
AC_PROG_LIBTOOL

dnl NEVER remove the $CXXFLAGS from the CXXFLAGS setting
if test $ac_cv_prog_gxx = yes; then
  CXXFLAGS="$CXXFLAGS -Wall -Werror"
fi

AC_OUTPUT(Makefile base/Makefile interface/Makefile modules/Makefile display/Makefile)
