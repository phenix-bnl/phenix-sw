AC_INIT(configure.in)

AM_INIT_AUTOMAKE(lvl2, 1.00)

AC_PROG_CXX(CC g++)
AC_ENABLE_STATIC(no)
AC_PROG_LIBTOOL

dnl Add -Wall to CXXFLAGS if g++ compiler
if test $ac_cv_prog_gxx = yes; then
  CXXFLAGS="$CXXFLAGS -Wall"
fi

AC_CHECK_LIB([pthread],[pthread_self])

AC_OUTPUT(Makefile)
