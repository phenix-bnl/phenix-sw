//************************************************************
// Parameter set-up macro written by the Pyrite GUI for PHOOL 
//************************************************************

{

/*

// set packform to 1 for simulated files (MDC-2 or VRDC)
// and to 0 for VRDC simulated files or Real data
int packform = 1;

  // Setting dTecCalibPar Parameters
  size_t nrc=1;
  dTecCalibPar->SetRowCount(nrc);
  dTecCalibPar->set_Verbose(0,0);

  // Setting dTecRemapPar Parameters
  size_t nrc=1;
  dTecRemapPar->SetRowCount(nrc);
  dTecRemapPar->set_Verbose(0,0);

  // Setting dTecAlignPar Parameters
  size_t nrc=1;
  dTecAlignPar->SetRowCount(nrc);
  dTecAlignPar->set_Verbose(0,0);

  // Setting dTecUnpackPar Parameters
  size_t nrc=1;
  dTecUnpackPar->SetRowCount(nrc);
  dTecUnpackPar->set_Verbose(0,0);
  dTecUnpackPar->set_Suppress(0,0);
  dTecUnpackPar->set_UseListSum(0,0);
  dTecUnpackPar->set_Write2File(0,0);
  dTecUnpackPar->set_UseOldFormat(0,packform);

  // Setting dTecHoughTrackPar Parameters
  size_t nrc=1;
  dTecHoughTrackPar->SetRowCount(nrc);
  dTecHoughTrackPar->set_RandSeed(0,-123456789);
  dTecHoughTrackPar->set_Statistics(0,200000);
  dTecHoughTrackPar->set_fillRawTrack(0,1);
  dTecHoughTrackPar->set_whichplane(0,0);
  dTecHoughTrackPar->set_algorithm(0,0);
  dTecHoughTrackPar->set_index(0,-1234);
  dTecHoughTrackPar->set_Rebin(0,1);
  dTecHoughTrackPar->set_Verbose(0,0);
  dTecHoughTrackPar->set_Write2File(0,0);

  dTecHoughTrackPar->set_minbins(0,120);
  dTecHoughTrackPar->set_angLim(0,0.6);
  dTecHoughTrackPar->set_clPar(0,0,0.20);
  dTecHoughTrackPar->set_clPar(1,0,0.35);
  dTecHoughTrackPar->set_clPar(2,0,0.35);
  dTecHoughTrackPar->set_clPar(3,0,0.75);
  dTecHoughTrackPar->set_clPar(4,0,5.0);
  dTecHoughTrackPar->set_y11(0,-0.20);
  dTecHoughTrackPar->set_y12(0,0.20);
//     dTecHoughTrackPar->set_minbins(0,70);
//     dTecHoughTrackPar->set_angLim(0,1.0);
//     dTecHoughTrackPar->set_clPar(0,0,0.20);
//     dTecHoughTrackPar->set_clPar(1,0,0.35);
//     dTecHoughTrackPar->set_clPar(2,0,0.35);
//     dTecHoughTrackPar->set_clPar(3,0,0.75);
//     dTecHoughTrackPar->set_clPar(4,0,5.0);
//     dTecHoughTrackPar->set_y11(0,-0.30);
//     dTecHoughTrackPar->set_y12(0,0.10);

  dTecHoughTrackPar->set_clPar(5,0,2.0);
  dTecHoughTrackPar->set_Radius0(0,450.0);
  dTecHoughTrackPar->set_Dist0(0,0.22);
  dTecHoughTrackPar->set_m1(0,400);
  dTecHoughTrackPar->set_m2(0,150);
  dTecHoughTrackPar->set_y21(0,-1.2);
  dTecHoughTrackPar->set_y22(0,1.2);
  dTecHoughTrackPar->set_fillVector(0,1);
  dTecHoughTrackPar->set_fillVectTrack(0,1);
  dTecHoughTrackPar->set_Refit(0,1);

*/

// mTecHoughTrack->set_Write2File(0,1);
// mTecHoughTrack->set_INDEX(0,11);

}

