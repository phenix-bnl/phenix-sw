AC_INIT(configure.in)

AC_CANONICAL_HOST

AM_INIT_AUTOMAKE(datamanager_plugins_pg,1.0)
AC_PROG_CXX(CC g++)
AC_ENABLE_STATIC(no)
AC_PROG_LIBTOOL

if test $ac_cv_prog_gxx = yes; then
   CXXFLAGS="$CXXFLAGS -Wall -Werror"
fi

AC_OUTPUT(Makefile)
